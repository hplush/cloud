# Managed by Ansible
[Unit]
Description=Remove the old images every day

[Timer]
OnCalendar=daily
# The users should not clean up their images at the same second
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
