# Managed by Ansible
[Service]
# Caddy keeps the admin API socket here. Only Caddy itself can enter
# the directory, so website users can’t reach the socket.
RuntimeDirectory=caddy
RuntimeDirectoryMode=0700
