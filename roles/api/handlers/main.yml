---
- name: Restart the deploy API
  become: true
  become_user: '{{ api_user }}'
  environment:
    XDG_RUNTIME_DIR: /run/user/{{ api_uid }}
  ansible.builtin.systemd_service:
    name: api.service
    scope: user
    state: restarted
    daemon_reload: true
